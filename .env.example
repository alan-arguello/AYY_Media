# Public origin of this NEW website, with https:// and no path.
# Set before building for production. Vercel's production URL is a fallback.
SITE_URL=

# SAME Supabase project as consulting_website. Do not create a new project.
NEXT_PUBLIC_SUPABASE_URL=https://rcvmfvacalolcijkrvsb.supabase.co
# Reuse the existing server key from the original deployment.
# Never use NEXT_PUBLIC_ for the secret key. Do not commit real credentials.
SUPABASE_SECRET_KEY=
# Legacy alternatives are also supported: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY.

# Development only: optional location for local test registrations.
# SUMMIT_STORAGE_DIR=
